import 'package:flutter/material.dart';
import 'package:shoppos/features/settings/presentation/widgets/row_widget.dart';

class SettingsPages extends StatelessWidget {
  const SettingsPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Material(child: const RowWidget());
  }
}
